# Generated by the aivar orchestration agent
# Flow ID: f3
# Flow name: login fails with empty username
# Regenerate rather than edit by hand

import os
from playwright.sync_api import Page, expect

def test_login_fails_with_empty_username(page: Page):
    page.goto('https://www.saucedemo.com')
    # action: Username
    page.locator('[data-test="username"]').first.fill("")
    # action: Password
    page.locator('[data-test="password"]').first.fill("")
    # action: Login
    page.locator('[data-test="login-button"]').first.click()
    # assertion: Username is required
    expect(page.locator('[data-test="error"]').first).to_be_visible()
